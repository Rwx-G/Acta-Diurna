# Story 10.4: Per-Block-Type Editing - Reporting and Rich Blocks

## Status

Review

## Story

As the author,
I want the Epic 7 reporting and rich blocks to be editable in the WYSIWYG editor,
So that comparison matrices, callouts, lists, timelines and the rest are authored visually, not hand-written JSON.

## Acceptance Criteria

**Given** a scale-driven block (comparison-matrix, set-membership, legend, chip-cluster, timeline)
**When** edited
**Then** scale and entry references are picked from the document's declared scales (never raw keys typed blind), a set-membership block picks its `sourceBlockId` from the document's comparison-matrix blocks, and an unknown or dangling reference is the actionable validation error naming the offending reference (FR2 parity) - the editor reuses the existing per-block editors for these

**Given** a content block (callout, code, card-grid, field-grid, list)
**When** edited
**Then** tone / icon / items / layout are edited in place through their existing editors, icon pickers offer only the curated registry names (Epic 7.6), and the preview renders the SSR result

**Given** a block whose authoring is genuinely complex (the comparison-matrix findings grid)
**When** scoped
**Then** the editor covers the structured findings editing the existing `ComparisonMatrixBlockEditor` already provides, and any deferred refinement (e.g. bulk paste of findings) is named explicitly here as deferred, not silently missing

**Given** any reporting/rich-block edit
**When** saved
**Then** it is the same validate-on-write path and the preview is the authoritative render - no block type has a side path

## Tasks / Subtasks

- [x] **Run-bearing rich blocks edit their rich text as inline RUNS, not flattened text (ui).** Extracted the 10.3 text-block run-level editor to a shared `ParagraphsEditor` (text input per run + the schema mark toggles + the http(s) link input, runs and paragraphs add / remove / reorder), and refactored `TextBlockEditor` to delegate to it. Replaced the 1.5 flatten-on-edit textareas in `CalloutBlockEditor` (body), `ListBlockEditor` (item description) and `TimelineBlockEditor` (milestone detail) with `ParagraphsEditor`, so a callout / list / timeline now carries the schema's bold / italic / inline-code / link marks (the SAME vocabulary `InlineRuns.svelte` renders), the preview reflecting every toggle. No contenteditable, no HTML path: an HTML-looking run text is stored as the literal string and rendered escaped (renderer-purity, NFR14). The optional rich-text fields (list description, milestone detail) are added and removed as a whole; while present, the inner Remove floors at one paragraph.
- [x] **Scale-driven blocks pick refs from the DECLARED scales (ui).** Confirmed and tested that `ComparisonMatrixBlockEditor`, `LegendBlockEditor`, `ChipClusterBlockEditor`, `TimelineBlockEditor` and `SetMembershipBlockEditor` already pick the scale / entry / source references from the document's declared scales (a `<select>` of declared scale keys, the entries resolved via `resolveScaleRef`), never a free-typed dangling key. A set-membership block picks its `sourceBlockId` from the document's comparison-matrix blocks (threaded down by `ReportEditor`). An unknown or dangling reference (a scale later removed, or a freshly-added block whose starter ref is empty) is the actionable validation error naming the offending reference: the existing `validateScaleReferences` document-level cross-reference pass produces it as the optimistic inline guidance and the server 422 (FR2 parity).
- [x] **Comparison-matrix (the most complex rich block) findings grid (ui).** The existing `ComparisonMatrixBlockEditor` covers the structured findings authoring (per finding: category, label, severity from the severity scale, optional tag, the per-source cells - state + note, one row per sources-scale entry - and the treatment before / after / status; plus the block-level severity / source scale refs). This story adds keyboard-accessible finding REORDER (move up / down via `moveItem`), preserving the Epic 11 `linkTo` on every edit (the editor mutates findings in place and never touches the field it does not surface). SCOPED: bulk paste of findings (a grid import) is explicitly DEFERRED, not silently missing.
- [x] **Content blocks (ui).** `CalloutBlockEditor` (tone enum + icon registry + kicker + run-bearing body), `CodeBlockEditor` (language + escaped source + annotations), `CardGridBlockEditor` (columns + icon-registry / title / description cards), `FieldGridBlockEditor` (the grid / strip meta-strip layout variant + label / value items), `ListBlockEditor` (ordered / unordered + term + run-bearing description) edit in place through their existing editors; the icon pickers offer only the curated `ICON_NAMES` registry (Epic 7.6).
- [x] **The single validated-save seam (covered by 10.1).** Every field edit mutates the in-edit `$state` document and calls `onEdit()` -> the SAME debounced live preview + optimistic validation + validated save 10.1 established. No parallel model, no second write path; an incomplete/invalid field is the optimistic inline guidance, then the server 422 on save - never half-saved.
- [x] **Accessibility (ui, NFR15 / WCAG 2.5.3).** Every field carries a label (`aria-label` or `<label>`); the mark toggles are real checkboxes; the run / finding / milestone move controls carry the full descriptive name in a component-scoped `.sr-only` span with the visible glyph `aria-hidden` (label-in-name, the 10.3 pattern the shared `ParagraphsEditor` inherits). The rich-block editors are axe-clean (desktop e2e).
- [x] **Reader-budget isolation (ops).** The rich per-block editors are workspace-only: `pnpm reader:budget` confirms the reader path is byte-identical at 75.9 KB / 200 KB, and `pnpm schema:export` is unchanged (no schema change - these editors edit the SAME schema shapes).
- [x] **Reuse, not re-duplication.** The run-bearing blocks share one `ParagraphsEditor` (not three copies); the shared `form-fields.css` and the `RUN_MARKS` / `toggleRunMark` / `setRunLink` / `newRun` / `moveItem` helpers are reused. The now-unused `paragraphText` flatten helper (and its test) were removed.
- [x] **Tests.**
  - [x] Component (`rich-block-editors.svelte.test.ts`): callout body bold-mark toggle (a valid schema run, never HTML, no leak to other runs) + HTML-looking text stored literal; list add / edit / remove description; timeline status scale + entry from declared scales, entry cleared on scale change, milestone reorder, detail run editing; matrix severity from the declared scale, finding reorder preserving `linkTo`, per-source cell set; legend / chip-cluster / set-membership ref picked from declared options, and a dangling legend `scaleRef` / chip entry / set-membership `sourceBlockId` surfacing the document-validator 422 at the offending path.
  - [x] e2e (`editor-rich-blocks.e2e.ts`, desktop): seed a document declaring two scales + one of each rich block, edit a matrix finding + a callout body run (+ bold, preview renders `<strong>`) + a list item + a timeline milestone + a card; save; reload; every field edit persisted. A scale-driven block with an unresolved reference shows the inline error at the block. The rich-block editors are axe-clean.

## Dev Notes

### This REFINES already-mounted rich sub-editors on the 10.1 seam - it is not a from-scratch build

The 1.5 editor already shipped per-block-type sub-editors for the ten Epic 7 reporting/rich blocks, dispatched by `BlockEditor` (`ComparisonMatrixBlockEditor`, `SetMembershipBlockEditor`, `LegendBlockEditor`, `ChipClusterBlockEditor`, `CalloutBlockEditor`, `CodeBlockEditor`, `CardGridBlockEditor`, `FieldGridBlockEditor`, `ListBlockEditor`, `TimelineBlockEditor`). Story 10.1 wired the debounced live preview + optimistic validation + validated save; 10.2 added the palette + structural editing; 10.3 brought the five core editors up to the AC and introduced the run-level text editor + the `RUN_MARKS` / `toggleRunMark` / `setRunLink` / `newRun` helpers. Story 10.4's job was to bring the rich editors up to their AC on the same seam.

What EXISTED vs what was REFINED, per rich type:

- **comparison-matrix** - existed: the full structured findings grid (category, label, severity from the severity scale, tag, per-source cells, treatment; severity / source scale refs from the declared scales). Refined: added finding REORDER (move up / down) and an explicit note that the Epic 11 `linkTo` is preserved (the editor mutates in place, never touches the field it does not surface). Scoped: bulk paste of findings deferred.
- **set-membership** - existed and unchanged: picks `sourceBlockId` from the document's comparison-matrix blocks (a select), plus an optional title. The dangling-ref check is the existing cross-reference pass.
- **legend** - existed and unchanged: picks `scaleRef` from the declared scales, plus an optional title.
- **chip-cluster** - existed and unchanged: picks `scaleRef` from the declared scales, then each chip's entry from that scale's entries.
- **timeline** - existed: per-milestone `status` ({ scaleRef, entry }) picked from the declared scales (the entry select reset to empty on a scale change so a stale entry cannot dangle), label, date, detail. Refined: the detail is now run-bearing (shared editor), and milestones reorder (move up / down).
- **callout** - existed: tone enum, icon registry, kicker. Refined: the body is now run-bearing (shared editor), replacing the flatten-on-edit textarea.
- **code** - existed and unchanged: language, escaped source (monospace, whitespace-preserving), optional annotations (note + optional line).
- **card-grid** - existed and unchanged: desktop column count, cards (icon registry / title / description), add / remove.
- **field-grid** - existed and unchanged: the grid / strip (meta-strip) layout variant, label / value items, add / remove.
- **list** - existed: ordered / unordered, item term. Refined: the optional description is now run-bearing (shared editor), added / removed as a whole.

### The run-marks reuse - one shared `ParagraphsEditor`, no freeform HTML

The four run-bearing surfaces (text block body, callout body, list-item description, timeline-milestone detail) edit the SAME `Paragraph[]` shape. Rather than copy the 10.3 run markup into each editor, it is extracted to one `ParagraphsEditor.svelte` that the text block delegates to (no field prefix, the bare "Paragraph N" accessible-name scheme kept byte-stable) and the three rich editors mount with a `label` prefix (e.g. "Callout body") so several run-bearing fields on one block stay distinguishable. The only formatting path is the boolean mark toggles (`bold` / `italic` / `code`) and the http(s) `link` href - the exact `inlineRunSchema` vocabulary - so a run produced here is always a valid schema run, never arbitrary markup (a component test toggles a mark and re-validates the document, another stores HTML-looking text as a literal string; the e2e toggles bold on a callout body and asserts the preview renders a `<strong>`).

### The scale-driven scaleRef-from-declared-scales approach, and how a dangling ref is caught

A scale-driven editor never offers a free text input for a scale / entry / source ref: it offers a `<select>` of the document's DECLARED scales (threaded down from `ReportEditor` as `scales` / `matrixBlocks`), the entry options resolved from the chosen scale via `resolveScaleRef`. So the editor cannot CREATE a dangling ref by typing one. A dangling ref still arises two legitimate ways - a scale referenced by a block is later removed from the document, or a freshly-added scale-driven block's starter ref is empty (not slug-valid) - and BOTH are caught by the existing document-level `validateScaleReferences` cross-reference pass: it walks the blocks, resolves each ref against `document.scales` (and a set-membership `sourceBlockId` against the comparison-matrix block ids), and emits a `{ path, message, hint }` naming the offending ref. That issue is the optimistic inline guidance (the client runs the SAME `documentSchemaV1` including the cross-reference passes) and, on save, the authoritative server 422 (FR2 parity). The component tests pin the 422 path for a dangling legend `scaleRef`, a chip entry, and a set-membership `sourceBlockId`; the e2e shows the inline error on a freshly-added scale-driven block whose starter ref is unresolved.

### The matrix-grid scoping decision

The comparison-matrix is the most complex rich block. The 10.4 deliverable is the STRUCTURED per-finding authoring (add / remove / edit findings and sources, set cells, the scale refs) the existing editor already provides, plus finding reorder added here. Bulk paste of findings (pasting a tabular grid of findings at once) is explicitly DEFERRED - it is a productivity refinement, not the core authoring, and is named here rather than silently missing. The per-source cells are keyed by the sources-scale entry keys and rendered one row per declared entry (the render tier iterates the scale order, not the record's own key order), so the editor surfaces exactly the columns the scale declares; there is no separate "add source" affordance because a source IS a sources-scale entry.

### Every editor reuses the 10.1 validated-save seam

No editor was given a save path. Each field mutation calls the `onEdit()` prop threaded down from `ReportEditor` (`BlockEditor` -> per-type editor), which sets `dirty` and schedules the debounced autosave; the live preview and optimistic validation both read the same debounced `settledSnapshot`. The save serializes `$state.snapshot(doc)` and posts it through `?/save` -> `updateReportDocument` (validate-on-write) with `expectedUpdatedAt`. So an invalid field is the optimistic inline guidance and then the authoritative server 422 - never a half-saved document.

### Reader-budget unchanged, schema unchanged

These editors are mounted only on the workspace edit route, behind the author cookie. `pnpm reader:budget` is byte-identical at 75.9 KB / 200 KB and `pnpm schema:export` shows no diff: 10.4 edits the SAME schema shapes the catalogue already defines, adding no block type and no field.

### Seams for 10.5, 10.6, 10.7

- **10.5 (binding from the editor):** untouched. The rich blocks edited here are author-written structured content (the matrix findings are NOT routed through the 2.4 flat-CSV binding path; the block has no `binding` field), so this story does not surface `bind` / `rebind` / `remap`. The bindable core editors (table / chart / kpi) already embed `BindingEditor`; 10.5 surfaces the existing binding actions at the block.
- **10.6 (audience-aware preview, tags, speaker notes):** the shared `BlockEditor` frame already mounts the `AudiencePicker` per block; 10.6 adds the level-switched preview and the section speaker notes. Untouched here.
- **10.7 (autosave / undo-redo / conflict / publish):** the single `scheduleSave` debounce, the `expectedUpdatedAt` concurrency token, and the publish / unpublish lifecycle are already wired in `ReportEditor`; 10.7 adds the undo / redo history and the explicit autosave UX. Untouched here.

## Dev Agent Record

### Debug Log

- Shared `ParagraphsEditor` accessible names: the text block keeps the bare "Paragraph N" / "Paragraph N, run M text" scheme (no field prefix) so the 10.3 e2e (`Paragraph 1, run 1 text`, `Paragraph 1, run 1 Bold`) is byte-stable; the rich editors pass a `label` prefix ("Callout body", "Item N description", "Milestone N detail"). The 10.3 text-block component test queried `Add run` (now the label-in-name `Add run to paragraph 1`); that single query was updated for the better accessible name, no behaviour change.
- The list description and timeline detail were the trickiest refactor: the old editor dropped the whole `description` / `detail` field when its last paragraph was removed. The new flow keeps the field add / remove as a whole-field action ("Add description" / "Remove description") and floors the inner `ParagraphsEditor` Remove at one paragraph, so the field is either absent or a valid non-empty paragraph array - never an empty-array `.min(1)` violation.
- `paragraphText` (the 1.5 flatten helper) became dead once the three editors moved to run editing; removed it and its unit test rather than leave dead code. Its only non-test consumers were the three refactored editors.
- e2e seed: the editor has no scales-declaration UI in scope, so a scale-driven block test needs the scales seeded another way. Added a `seedDocument` helper that posts a full document through the editor's OWN `?/save` action (the single validated write path), so the seed is validated like any other write - no second seam.

### Completion Notes

- All ten rich editors meet the 10.4 AC on the 10.1 validated-save seam; no new schema, no new write path, no new dependency.
- Gates (local, no CI): `pnpm test` 1982 passed / 170 files, 0 failed; `pnpm build && pnpm test:e2e` 141 passed, 40 skipped, 0 failed (desktop + mobile; mobile skips the desktop-only workspace specs and the single-mode reader specs); `pnpm test:e2e:multi` 11 passed, 0 failed; `pnpm lint` clean; `pnpm check` 0 errors / 0 warnings; `pnpm reader:budget` 75.9 KB / 200 KB (unchanged); `pnpm schema:export` no diff. The new rich-blocks e2e was re-run 3x with no flake.

## File List

- `src/routes/(workspace)/reports/[id]/edit/ParagraphsEditor.svelte` - new: shared inline-run paragraph editor (text input + mark toggles + link, run / paragraph add / remove / reorder), the 10.3 text run editor extracted for reuse by the run-bearing blocks (new)
- `src/routes/(workspace)/reports/[id]/edit/TextBlockEditor.svelte` - delegate to `ParagraphsEditor` (no field prefix), keeping the run-editing markup in one place (modified)
- `src/routes/(workspace)/reports/[id]/edit/CalloutBlockEditor.svelte` - body edits as inline runs through `ParagraphsEditor` (was a flatten-on-edit textarea); unused CSS dropped (modified)
- `src/routes/(workspace)/reports/[id]/edit/ListBlockEditor.svelte` - item description edits as inline runs through `ParagraphsEditor`, added / removed as a whole field; unused CSS dropped (modified)
- `src/routes/(workspace)/reports/[id]/edit/TimelineBlockEditor.svelte` - milestone detail edits as inline runs through `ParagraphsEditor`; milestone reorder (move up / down); unused CSS dropped (modified)
- `src/routes/(workspace)/reports/[id]/edit/ComparisonMatrixBlockEditor.svelte` - finding reorder (move up / down), explicit `linkTo`-preservation and matrix-scoping notes (modified)
- `src/routes/(workspace)/reports/[id]/edit/editor-state.ts` - removed the now-unused `paragraphText` flatten helper and the unused `Paragraph` import (modified)
- `src/routes/(workspace)/reports/[id]/edit/editor-state.test.ts` - removed the `paragraphText` test + import (modified)
- `src/routes/(workspace)/reports/[id]/edit/text-block-editor.svelte.test.ts` - update the `Add run` query to the new label-in-name accessible name (modified)
- `src/routes/(workspace)/reports/[id]/edit/rich-block-editors.svelte.test.ts` - component coverage for the rich-block field editing (callout / list / timeline / matrix / legend / chip-cluster / set-membership) (new)
- `e2e/helpers.ts` - add the `seedDocument` helper (post a full document through the editor `?/save` action) (modified)
- `e2e/editor-rich-blocks.e2e.ts` - rich-block field editing, persistence, unresolved-ref inline error, axe (new)
- `CHANGELOG.md` - `[Unreleased]` entry for story 10.4 (modified)
- `docs/stories/10.4.md` - this story file (new)

## Change Log

- 2026-06-15: Extracted the 10.3 text run-level editor to a shared `ParagraphsEditor`; the callout body, list-item description and timeline-milestone detail now edit their rich text as inline runs with the schema's bold / italic / inline-code / link marks (no more flatten-on-edit, no freeform HTML). (Romain G.)
- 2026-06-15: Comparison-matrix finding reorder (move up / down) preserving the Epic 11 `linkTo`; bulk paste of findings explicitly deferred. Timeline milestone reorder. (Romain G.)
- 2026-06-15: Confirmed and tested the scale-driven ref pickers (matrix / set-membership / legend / chip-cluster / timeline) pick from the declared scales, an unresolved ref surfacing as the cross-reference inline guidance and the server 422 (FR2 parity). (Romain G.)
- 2026-06-15: Removed the now-unused `paragraphText` flatten helper and its test. (Romain G.)
- 2026-06-15: Component + desktop e2e coverage for rich-block field editing; reader budget unchanged (75.9 KB / 200 KB), schema unchanged. (Romain G.)
- 2026-06-15: Status -> Review. (Romain G.)
