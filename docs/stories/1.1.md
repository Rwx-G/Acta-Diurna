# Story 1.1: Project Scaffold & Quality Gates

Status: Review

## Story

As the author-developer,
I want a scaffolded SvelteKit project with quality gates,
So that every future story builds on verified foundations.

## Acceptance Criteria

**Given** a clean clone
**When** `pnpm install && pnpm build && pnpm check && pnpm test` runs
**Then** all commands succeed on Node 22 with strict TypeScript and exact-pinned dependencies
**And** the build (scaffold per AR1 + `@sveltejs/adapter-node`) produces a runnable Node server

**Given** a push to GitHub
**When** CI runs
**Then** lint, svelte-check, vitest and build jobs pass (AR8)

## Tasks / Subtasks

- [x] Scaffold SvelteKit at repo root via `npx sv create` (minimal template, TypeScript; add-ons: eslint, prettier, vitest, playwright) - scaffold in a temp dir and merge into the existing repo root without touching docs/, .claude/, _bmad*, .github/
- [x] Swap adapter to `@sveltejs/adapter-node`; enable `strict` in tsconfig; pin exact dependency versions; add `.nvmrc` (22) and `engines` field
- [x] Wire root `.gitignore` additions for SvelteKit outputs if missing (node_modules, .svelte-kit, build)
- [x] Add a minimal landing route so build output is verifiable
- [x] GitHub Actions CI: lint + svelte-check + vitest + build (`.github/workflows/ci.yml`)
- [x] Verify all four commands pass locally; record versions in Dev Agent Record

## Dev Notes

- Architecture authority: `docs/prd/architecture.md` - "Starter Template Evaluation" (AR1 command), "Core Architectural Decisions" (D14 adapter-node), "Implementation Patterns" (naming, structure).
- Host environment: Windows + Git Bash; Node 25 on host (target remains Node 22 - pin via `.nvmrc`/`engines`, Docker image enforces 22); pnpm 11.5.3 at `/c/Users/Romain/AppData/Roaming/npm/pnpm` (add to PATH in commands: `export PATH="$PATH:/c/Users/Romain/AppData/Roaming/npm"`).
- `sv create` may refuse a non-empty directory: scaffold into `tmp-scaffold/`, move contents to root, delete temp dir.
- Playwright e2e comes with the add-on; keep its config but e2e specs arrive in later stories (do not delete the harness).
- Do NOT touch existing repo files: README.md, LICENSE, docs/**, .github/ISSUE_TEMPLATE, PULL_REQUEST_TEMPLATE (CI workflow is additive).

## Dev Agent Record

### Debug Log

- Scaffold (fully non-interactive, sv v0.16.1): `npx sv create tmp-scaffold --template minimal --types ts --add prettier eslint "vitest=usages:unit,component" playwright "sveltekit-adapter=adapter:node" --no-install --no-download-check` -> "Successfully setup add-ons: prettier, eslint, vitest, playwright, sveltekit-adapter". Merged into root with `cp -a`, scaffold README.md and .gitignore discarded (root keeps its own).
- `pnpm install` (pnpm 11.5.3, host Node v25.4.0): 268 packages resolved, done in 4.4s. Exact versions read back via `pnpm list --depth 0`, then pinned in package.json (no caret) and lockfile confirmed in sync ("Already up to date").
- `pnpm build`: client + server bundles built, "Using @sveltejs/adapter-node ... done"; `build/` contains `index.js`, `handler.js`, `env.js`, `shims.js`, `client/`, `server/` (runnable Node server).
- `pnpm check`: svelte-check COMPLETED 348 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS.
- `pnpm lint`: "All matched files use Prettier code style!" + eslint clean (verified with pipefail, exit 0). Required `.prettierignore` additions for pre-existing prose (docs/, .claude/, _bmad*, README.md, LICENSE, .github templates).
- `pnpm test`: chromium downloaded by `playwright install chromium`, Vitest "Test Files 1 passed (1), Tests 1 passed (1)", then `playwright test --pass-with-no-tests` exited 0 (zero e2e specs by design).
- Key versions: svelte 5.56.3, @sveltejs/kit 2.64.0, vite 8.0.16, @sveltejs/adapter-node 5.5.4, @sveltejs/vite-plugin-svelte 7.1.2, vitest 4.1.8, @playwright/test 1.60.0, typescript 6.0.3, eslint 10.4.1, prettier 3.8.4, svelte-check 4.6.0.

### Completion Notes

- No `svelte.config.js`: current sv scaffolds configure SvelteKit (including the adapter) through the `sveltekit()` plugin options in `vite.config.ts`. The architecture tree lists `svelte.config.js`; treat `vite.config.ts` as the config home unless a future need forces a split.
- tsconfig was already `strict: true` from the scaffold; verified, not added.
- Scaffold demo content (src/routes/demo/, src/lib/vitest-examples/) was removed. The landing route `src/routes/+page.svelte` plus co-located `src/routes/page.svelte.test.ts` (Vitest browser mode, chromium) replace it with real, non-placeholder coverage.
- Playwright decision: harness kept (`playwright.config.ts`, `testDir: 'e2e'`, `e2e/.gitkeep`); `test:e2e` runs `playwright test --pass-with-no-tests` so zero specs do not fail locally or in CI. First e2e story should drop the flag or simply add specs (flag is harmless once specs exist).
- `pnpm test` starts with `playwright install chromium` so the AC's clean-clone run works without a manual browser install (no-op when cached). CI additionally runs `playwright install --with-deps chromium` for runner system deps.
- Vitest is split in two projects (scaffold default): `client` (browser mode, `*.svelte.test.ts`, excludes `src/lib/server/**`) and `server` (node env, other `*.test.ts`). Story 1.2+ unit tests for server code need no browser.
- Prettier owns app code only: `.prettierignore` excludes docs/, .claude/, _bmad*, README.md, LICENSE and .github templates so `pnpm lint` does not reformat pre-existing repo prose.
- `.npmrc` is the scaffold-generated one; the local secrets-fence hook blocks reading it by name, so it was committed as generated, unmodified and uninspected.
- CI (`.github/workflows/ci.yml`): four jobs (lint, check, test, build) on push to main + PRs, Node from `.nvmrc`, pnpm version from the `packageManager` field via pnpm/action-setup@v4. Not verifiable locally; first push will prove it.

## File List

- Created: package.json, pnpm-lock.yaml, .npmrc, .nvmrc, .prettierrc, .prettierignore, eslint.config.js, tsconfig.json, vite.config.ts, playwright.config.ts, e2e/.gitkeep, .vscode/extensions.json, src/app.d.ts, src/app.html, src/lib/index.ts, src/lib/assets/favicon.svg, src/routes/+layout.svelte, src/routes/+page.svelte, src/routes/page.svelte.test.ts, static/robots.txt, .github/workflows/ci.yml
- Modified: .gitignore (.env.example exception, Vite timestamp files, Playwright outputs), docs/stories/1.1.md

## Change Log

- 2026-06-12: Story created from epics.md (Epic 1).
- 2026-06-12: Scaffolded SvelteKit (minimal, TS) with eslint/prettier/vitest/playwright add-ons and adapter-node; pinned exact deps; Node 22 via .nvmrc + engines; landing route + browser test; CI pipeline added. All four AC commands pass locally. Status -> Review.
