// Re-exported from the shared documents module so the workspace action and the
// `/api/v1/reports` endpoint seed a new draft with the SAME default title (one
// authoring constant, two surfaces - story 4.2 parity).
export { DEFAULT_REPORT_TITLE } from '$lib/server/documents/defaults';
